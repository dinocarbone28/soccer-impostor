<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1" />
  <title>Soccer Impostor</title>
  <link rel="stylesheet" href="styles.css" />
</head>
<body>
  <!-- Red badge visible only to the impostor -->
  <div id="impostorBadge" class="badge hide">IMPOSTOR</div>

  <div class="wrap">
    <header>
      <h1>⚽ Soccer Impostor</h1>
      <p class="sub">Create a room, invite friends, and try to find the impostor.</p>
    </header>

    <!-- HOME (Host or Join) -->
    <section id="home">
      <div class="card">
        <h2>Host a Game</h2>
        <label>Display Name</label>
        <input id="hostName" placeholder="Your name" maxlength="20" />
        <label>Max Players (3–12)</label>
        <input id="maxPlayers" type="number" min="3" max="12" value="8" />
        <button id="btnHost">Create Room</button>
      </div>

      <div class="divider">or</div>

      <div class="card">
        <h2>Join a Game</h2>
        <label>Room Code</label>
        <input id="joinCode" placeholder="e.g., ABCD" maxlength="4" />
        <label>Display Name</label>
        <input id="joinName" placeholder="Your name" maxlength="20" />
        <button id="btnJoin">Join Room</button>
      </div>
    </section>

    <!-- LOBBY -->
    <section id="lobby" class="hide">
      <div class="card">
        <h2>Lobby <span id="roomCodeBadge"></span></h2>
        <p id="shareLinkRow" class="muted"></p>
        <ul id="playerList"></ul>
        <div id="hostControls" class="hostOnly hide">
          <button id="btnStart">Start Game</button>
        </div>
        <button id="btnCopyLink" class="secondary">Copy Join Link</button>
        <button id="btnBackHome" class="secondary">Leave Room</button>
      </div>
    </section>

    <!-- ROLE REVEAL -->
    <section id="role" class="hide">
      <div class="card">
        <h2>Your Role</h2>
        <p id="roleText"></p>
        <p id="secretText" class="secret"></p>
        <p id="roundInfo" class="muted"></p>
      </div>
    </section>

    <!-- DESCRIBING PHASE -->
    <section id="describe" class="hide">
      <div class="card">
        <h2>Description Round</h2>

        <!-- BIG SECRET NAME for innocents -->
        <h1 id="secretBig" class="secret-big hide"></h1>

        <p class="muted">Give a short hint. Keep it vague if you are the impostor!</p>
        <input id="descInput" placeholder="One short hint (max 80 chars)" maxlength="80" />
        <button id="btnSubmitDesc">Submit Hint</button>
        <p id="subCount" class="muted"></p>

        <!-- everyone sees submitted hints (anonymous) -->
        <ul id="hintList" class="hint-list"></ul>

        <div id="hostControlsDescribe" class="hostOnly hide">
          <button id="btnNextPhase">Next Round / Go to Vote</button>
        </div>
      </div>
    </section>

    <!-- VOTING PHASE -->
    <section id="vote" class="hide">
      <div class="card">
        <h2>Vote the Impostor</h2>

        <!-- Keep big secret visible for innocents during voting too -->
        <h1 id="secretBigVote" class="secret-big hide"></h1>

        <ul id="voteList"></ul>
        <p id="voteCount" class="muted"></p>
        <div id="hostControlsVote" class="hostOnly hide">
          <button id="btnEndVoting">End Voting & Reveal</button>
        </div>
      </div>
    </section>

    <!-- FINAL GUESS (IMPOSTOR) -->
    <section id="finalGuess" class="hide">
      <div class="card">
        <h2>Final Guess (Impostor)</h2>
        <p class="muted">Type the exact player name.</p>
        <input id="guessInput" placeholder="Exact name" />
        <button id="btnGuess">Submit Guess</button>
      </div>
    </section>

    <!-- GAME OVER -->
    <section id="results" class="hide">
      <div class="card">
        <h2>Results</h2>
        <p id="resultText"></p>
        <p id="revealSecret" class="secret"></p>
        <div id="hostControlsResults" class="hostOnly hide">
          <button id="btnRestart">Back to Lobby</button>
        </div>
      </div>
    </section>

    <footer>
      <span id="toaster"></span>
    </footer>
  </div>

  <script src="/socket.io/socket.io.js"></script>
  <script src="client.js"></script>
</body>
</html>
